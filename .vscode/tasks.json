{
    "tasks": [
        {
            "label": "preRun",
            "type": "shell",
            "windows":{
                "command": "clear & start openocd -c \"set ESP_RTOS none\" -f board/esp32s3-builtin.cfg & exit"
            }
        },
    ],
    "version": "2.0.0"
}